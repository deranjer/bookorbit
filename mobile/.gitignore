node_modules/
.expo/
dist/
android/
ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env

# The root .gitignore excludes package-lock.json (repo is pnpm), but the mobile
# app is npm-managed — keep its lockfile so CI `npm ci` and EAS builds are reproducible.
!package-lock.json
